import React, { Component } from 'react';

class Header extends Component {
    render() {
        return (
            <header>
                Some Header
            </header>
        );
    }
}

export default Header;